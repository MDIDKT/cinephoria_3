{% extends 'base.html.twig' %}

{% block title %}Place index{% endblock %}

{% block body %}
    <h1>Place index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Siege</th>
                <th>SiegeMobiliteReduite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for place in places %}
            <tr>
                <td>{{ place.id }}</td>
                <td>{{ place.siege }}</td>
                <td>{{ place.siegeMobiliteReduite ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_admin_place_show', {'id': place.id}) }}">show</a>
                    <a href="{{ path('app_admin_place_edit', {'id': place.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_place_new') }}">Create new</a>
{% endblock %}
